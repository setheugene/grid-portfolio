/**
 * tailwind.config.js — not used with Tailwind v4.
 *
 * Design tokens and theme configuration live in assets/css/input.css
 * inside the @theme {} block. This file is kept as a reference only.
 *
 * To build:
 *   ./tailwindcss -i assets/css/input.css -o assets/css/main.css --minify
 *
 * To watch:
 *   ./tailwindcss -i assets/css/input.css -o assets/css/main.css --watch
 */
